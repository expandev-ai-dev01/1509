import { Router } from 'express';

const router = Router();

// Public routes will be added here by feature implementations
// Example: Health check, public information endpoints

export default router;
