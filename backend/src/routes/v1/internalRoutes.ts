import { Router } from 'express';

const router = Router();

// Internal authenticated routes will be added here by feature implementations
// Example: Task management, user preferences, etc.

export default router;
